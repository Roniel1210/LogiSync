services:
  n8n:
    image: docker.n8n.io/n8nio/n8n:2.19.2
    container_name: logisync-n8n
    ports:
      - "5678:5678"
    environment:
      - N8N_HOST=localhost
      - N8N_PORT=5678
      - GENERIC_TIMEZONE=America/Santo_Domingo
      - TZ=America/Santo_Domingo
      - LOGISYNC_DATA_ROOT=/files
      - NODES_EXCLUDE=[]
      - N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true
      - N8N_RUNNERS_ENABLED=true
    volumes:
      - ./n8n/storage:/home/node/.n8n
      - ./data:/files
      - ./scripts:/workspace/scripts
    restart: unless-stopped
